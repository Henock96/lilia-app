import 'package:flutter/material.dart';

class buildFallbackSlider extends StatelessWidget {
  const buildFallbackSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
